---
summary: "CLI reference for `marketbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `marketbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
marketbot uninstall
marketbot uninstall --all --yes
marketbot uninstall --dry-run
```

